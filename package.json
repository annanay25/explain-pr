{
  "name": "figure",
  "private": true,
  "version": "0.1.0",
  "description": "A visual language for explaining pull requests as stepped HTML figures.",
  "type": "module",
  "scripts": {
    "dev": "vite --host --port 43147",
    "build": "vite build",
    "preview": "vite preview --host --port 43147",
    "capture": "node scripts/capture.mjs"
  },
  "devDependencies": {
    "playwright": "^1.55.0",
    "vite": "^6.3.5"
  }
}
